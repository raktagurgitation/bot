services:
  postgres:
    image: postgres:16-alpine
    container_name: botUsers
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: 123
      POSTGRES_DB: botDatabase
    ports:
      - '5454:5432'
    volumes:
      - pg_bot_data:/var/lib/postgresql/data

volumes:
  pg_bot_data: